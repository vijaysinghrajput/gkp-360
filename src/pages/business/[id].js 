import { useRouter } from 'next/router';

export default function BusinessDetail() {
  const router = useRouter();
  const { id } = router.query;

  return (
    <div>
      <h1>Business Details for ID: {id}</h1>
      <p>Information about the selected business.</p>
    </div>
  );
}
